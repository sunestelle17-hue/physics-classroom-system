{
  "name": "physics-classroom-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "node --check src/app.js && rm -rf dist && mkdir -p dist/src && cp index.html dist/index.html && cp src/app.js src/styles.css dist/src/",
    "preview": "python3 -m http.server 4173 --directory dist"
  }
}
